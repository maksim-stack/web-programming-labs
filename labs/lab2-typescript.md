# **Лабораторна робота №2**

**Тема:** Типізація проекту

**Мета:** Закріпити практичні навички роботи з TypeScript: базові типи та анотації, інтерфейси та type aliases, generics, utility types, ООП з модифікаторами доступу, type guards та класи з наслідуванням.

**Обладнання:** IBM-сумісні ПК

**Програмне забезпечення:** ОС Windows 10/11, macOS або Linux; Node.js (LTS); npm; VS Code з розширенням ESLint та TypeScript; обліковий запис GitHub; Git

## **Контрольні запитання**

1. Яка різниця між `interface` та `type`? Коли варто використовувати кожен?
2. Що таке generics і яку проблему вони вирішують?
3. Чим відрізняються `Partial<T>`, `Required<T>` та `Readonly<T>`?
4. Які модифікатори доступу є в TypeScript і в чому їх відмінності?
5. Що таке type guard? Наведіть приклади вбудованих type guards.

## **Теоретичні відомості**

### **Базові типи та анотації**

TypeScript додає статичну типізацію до JavaScript: типи перевіряються на етапі компіляції, що дозволяє виявляти помилки ще до запуску програми. Анотація типу записується через `:` після імені змінної або параметра. TypeScript також підтримує **виведення типів** (type inference) — якщо тип можна визначити з початкового значення, явна анотація не обов'язкова.

### **Інтерфейси та type aliases**

**Interface** описує структуру об'єкта та підтримує `extends` для розширення. **Type alias** (`type`) — псевдонім для будь-якого типу, включаючи union, tuple, conditional types. Обидва підходи взаємозамінні для об'єктних типів, але `interface` краще підходить для публічних контрактів, а `type` — для складних типових виразів.

### **Generics**

Generics (узагальнені типи) дозволяють створювати компоненти, що працюють з різними типами, зберігаючи типобезпеку. Замість дублювання коду або використання `any` — один узагальнений компонент.

### **Utility Types**

TypeScript надає вбудовані generic-типи для трансформації існуючих типів: `Partial<T>` робить усі поля необов'язковими, `Required<T>` — навпаки, `Readonly<T>` забороняє зміну, `Pick<T, K>` залишає лише обрані поля, `Omit<T, K>` виключає поля, `Record<K, V>` створює об'єкт з однотипними значеннями.

### **Класи та модифікатори доступу**

TypeScript розширює класи ES6 модифікаторами `public`, `private`, `protected` та `readonly`. Вони реалізують інкапсуляцію — приховування деталей реалізації за публічним інтерфейсом. TypeScript також підтримує скорочений синтаксис parameter properties: оголошення властивостей прямо в конструкторі.

### **Type Guards**

Type guard — вираз, що перевіряє тип у runtime і дозволяє TypeScript звужувати union type у відповідному блоці коду. Вбудовані guards: `typeof` для примітивів, `instanceof` для класів, `in` для перевірки наявності властивості. Користувацький guard оголошується через предикат `value is Type`.

## **Хід роботи**

У цій лабораторній роботі ви виконаєте 5 практичних завдань, що покривають ключові можливості TypeScript.

### **Етап 1: Підготовка репозиторію та структури проєкту**

1.1. Використайте репозиторій `web-programming-labs`, створений у попередній лабораторній роботі. Перейдіть у його локальну папку:

```
cd web-programming-labs
```

1.2. Створіть папку `lab2` з такою структурою:

```
web-programming-labs/
├── lab1/
│   └── ...
├── lab2/
│   ├── src/
│   │   ├── config.ts
│   │   ├── task-1.ts
│   │   ├── task-2.ts
│   │   ├── task-3.ts
│   │   ├── task-4.ts
│   │   └── task-5.ts
│   ├── package.json
│   ├── tsconfig.json
│   └── README.md
└── ...
```

1.3. Ініціалізуйте npm-проєкт та встановіть TypeScript:

```bash
cd lab2
npm init -y
npm install --save-dev typescript
```

1.4. Ініціалізуйте конфігурацію TypeScript:

```bash
npx tsc --init
```

1.5. Замініть вміст `lab2/tsconfig.json` на наступний:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "esModuleInterop": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

1.6. Додайте до `lab2/package.json` скрипти для зручної роботи:

```json
{
  "scripts": {
    "build": "tsc",
    "start": "node dist/task-1.js",
    "watch": "tsc --watch"
  }
}
```

1.7. Переконайтесь, що в корені репозиторію є файл `.gitignore` з наступним вмістом (якщо його ще немає — створіть):

```
node_modules/
dist/
```

Це запобігає потраплянню скомпільованих файлів та залежностей у репозиторій.

1.8. Створіть файл `lab2/src/config.ts` з вашим варіантом:

```typescript
// config.ts — Конфігурація варіанту
export const VARIANT: number = 0; // Ваш номер у журналі (1-30)
```

**Важливо:** значення `VARIANT` впливає на вхідні дані у завданні 2. Кожен студент повинен використовувати свій номер з журналу.

1.9. Зафіксуйте початкову структуру:

```bash
git add lab2/
git commit -m "Lab 2: Initial TypeScript project structure"
git push origin main
```

**Результат:** Репозиторій містить папку `lab2` з підготовленою структурою TypeScript-проєкту, а `node_modules/` та `dist/` ігноруються git.

---

### **Етап 2: Виконання завдань**

Для кожного завдання:

- Реалізуйте функції та класи відповідно до умови
- Додайте коментарі, що пояснюють логіку **своїми словами**
- Виведіть результати через `console.log()`
- Скомпілюйте код командою `npx tsc` — компіляція має завершитись **без помилок**
- Запустіть результат: `node dist/task-N.js`
- Зробіть **окремий коміт** після завершення кожного завдання

> **Важливо про модулі.** Файл TypeScript без жодного `import` або `export` трактується компілятором як глобальний скрипт, а не модуль. Це означає, що типи з різних файлів потрапляють в один простір імен і конфліктують — ви отримаєте помилку `Duplicate identifier`. Щоб уникнути цього, **кожен файл завдання, що не містить `import`, повинен починатися з рядка `export {};`**. Цей рядок нічого не експортує, але перетворює файл на ізольований модуль.

---

#### **Завдання 1: Базові типи, інтерфейси та type aliases (task-1.ts)**

Першим рядком файлу додайте `export {};`

**1.1.** Оголосіть type alias `Status` для допустимих статусів задачі:

```typescript
type Status = "todo" | "in_progress" | "done" | "cancelled";
```

**1.2.** Оголосіть type alias `Priority` для рівня пріоритету:

```typescript
type Priority = "low" | "medium" | "high" | "critical";
```

**1.3.** Оголосіть інтерфейс `Task` для задачі у системі управління:

```typescript
interface Task {
  id: number;
  title: string;
  description: string;
  status: Status;
  priority: Priority;
  assignee: string | null; // null, якщо задача не призначена
  createdAt: Date;
  dueDate: Date | null;
}
```

**1.4.** Оголосіть інтерфейс `Project`, що розширює базовий інтерфейс `HasId`:

```typescript
interface HasId {
  id: number;
}

interface Project extends HasId {
  name: string;
  description: string;
  tasks: Task[];
  ownerId: number;
}
```

**1.5.** Реалізуйте функцію `getTaskStats(tasks: Task[])`, що повертає об'єкт зі статистикою:

```typescript
// Очікуваний тип результату:
// { total: number; byStatus: Record<Status, number>; overdue: number }
```

- `total` — загальна кількість задач
- `byStatus` — кількість задач по кожному статусу
- `overdue` — кількість задач, у яких `dueDate` минув (менше за поточну дату) і статус не `"done"` та не `"cancelled"`

**1.6.** Реалізуйте функцію `formatTask(task: Task): string`, що повертає рядок у форматі `"[#id] Заголовок (пріоритет, статус)"`. Наприклад: `"[#1] Налаштувати CI/CD (high, in_progress)"`

Продемонструйте роботу функцій на масиві щонайменше з 5 задачами (різних статусів та пріоритетів), виведіть результати у консоль:

```typescript
console.log("=== Завдання 1: Базові типи, інтерфейси та type aliases ===");
// ... демонстрація
```

---

#### **Завдання 2: Generics та Utility Types (task-2.ts)**

На початку файлу імпортуйте конфігурацію:

```typescript
import { VARIANT } from "./config";
```

Наявність `import` вже робить файл модулем — `export {};` не потрібен. Оголосіть типи `Status`, `Priority` та інтерфейс `Task` у цьому файлі.

Створіть масив задач з даними, де числові поля залежать від варіанту:

```typescript
const tasks: Task[] = [
  {
    id: 1 + VARIANT,
    title: "Розробити API",
    description: "Реалізувати REST API для управління задачами",
    status: "in_progress",
    priority: "high",
    assignee: "Іван Петренко",
    createdAt: new Date("2025-01-10"),
    dueDate: new Date("2025-02-01"),
  },
  {
    id: 2 + VARIANT,
    title: "Написати тести",
    description: "Покрити unit-тестами основну логіку",
    status: "todo",
    priority: "medium",
    assignee: null,
    createdAt: new Date("2025-01-12"),
    dueDate: new Date("2025-02-15"),
  },
  {
    id: 3 + VARIANT,
    title: "Налаштувати БД",
    description: "Підключити PostgreSQL, виконати міграції",
    status: "done",
    priority: "critical",
    assignee: "Олена Коваль",
    createdAt: new Date("2025-01-05"),
    dueDate: new Date("2025-01-20"),
  },
  {
    id: 4 + VARIANT,
    title: "Оновити документацію",
    description: "Описати API у Swagger",
    status: "todo",
    priority: "low",
    assignee: null,
    createdAt: new Date("2025-01-15"),
    dueDate: null,
  },
  {
    id: 5 + VARIANT,
    title: "Code review",
    description: "Перевірити pull request від команди",
    status: "cancelled",
    priority: "medium",
    assignee: "Андрій Лисенко",
    createdAt: new Date("2025-01-18"),
    dueDate: new Date("2025-01-25"),
  },
];
```

Реалізуйте наступне, використовуючи generics та utility types:

**2.1.** Generic-інтерфейс `ApiResponse<T>`:

```typescript
interface ApiResponse<T> {
  data: T;
  status: number;
  message: string;
  timestamp: Date;
}
```

Створіть функцію `createSuccessResponse<T>(data: T): ApiResponse<T>` та `createErrorResponse<T>(message: string): ApiResponse<T | null>`.

**2.2.** `CreateTaskDto` та `UpdateTaskDto` — за допомогою utility types `Omit` та `Partial`:

- `CreateTaskDto` — тип для створення задачі: всі поля `Task`, крім `id` та `createdAt` (вони генеруються автоматично)
- `UpdateTaskDto` — тип для оновлення: всі поля `Task`, крім `id` та `createdAt`, але всі необов'язкові

**2.3.** Функція `filterTasks<K extends keyof Task>(tasks: Task[], key: K, value: Task[K]): Task[]` — фільтрує масив задач за будь-яким полем з типобезпекою.

Виведіть результати у консоль:

```typescript
console.log("=== Завдання 2: Generics та Utility Types ===");
console.log("Варіант:", VARIANT);
// ... демонстрація всіх функцій
```

---

#### **Завдання 3: Класи та модифікатори доступу (task-3.ts)**

Першим рядком файлу додайте `export {};`. Оголосіть типи `Status`, `Priority` та інтерфейс `Task`.

Реалізуйте ієрархію класів для системи управління задачами.

**3.1.** Клас `TaskManager`:

- Приватне поле `#tasks: Task[]` (ES2022 private field)
- Приватне поле `#nextId: number` з початковим значенням `1`
- Конструктор `constructor(initialTasks: Task[] = [])` — ініціалізує поля
- Публічний метод `addTask(dto: Omit<Task, "id" | "createdAt">): Task` — генерує `id` та `createdAt`, додає задачу, повертає її
- Публічний метод `updateTask(id: number, updates: Partial<Omit<Task, "id" | "createdAt">>): Task | null` — оновлює задачу, повертає оновлену або `null` якщо не знайдено
- Публічний метод `deleteTask(id: number): boolean` — видаляє задачу, повертає `true` при успіху
- Публічний getter `tasks: Task[]` — повертає копію масиву (через spread)
- Публічний getter `count: number` — повертає кількість задач
- Публічний метод `getById(id: number): Task | undefined`

**3.2.** Клас `FilteredTaskManager extends TaskManager`:

- Публічний метод `getByStatus(status: Status): Task[]`
- Публічний метод `getByPriority(priority: Priority): Task[]`
- Публічний метод `getByAssignee(assignee: string): Task[]`
- Публічний метод `getOverdue(): Task[]` — повертає прострочені задачі (аналогічно завданню 1.5)

Продемонструйте роботу:

```typescript
console.log("=== Завдання 3: Класи та модифікатори доступу ===");

const manager = new FilteredTaskManager();

const task1 = manager.addTask({
  title: "Розробити API",
  description: "REST API для задач",
  status: "in_progress",
  priority: "high",
  assignee: "Іван",
  dueDate: new Date("2025-02-01"),
});

console.log("Додано:", task1);
console.log("Кількість задач:", manager.count);

// ... додайте ще 3-4 задачі та продемонструйте всі методи
```

---

#### **Завдання 4: Наслідування та поліморфізм (task-4.ts)**

Першим рядком файлу додайте `export {};`.

Реалізуйте систему сповіщень з використанням абстрактного класу та поліморфізму.

**4.1.** Абстрактний клас `BaseNotifier`:

```typescript
abstract class BaseNotifier {
  constructor(protected readonly name: string) {}

  // Абстрактний метод — нащадки зобов'язані реалізувати
  abstract send(to: string, subject: string, body: string): void;

  // Шаблонний метод — спільна логіка для всіх нащадків
  notify(to: string, subject: string, body: string): void {
    console.log(`[${this.name}] Надсилання сповіщення...`);
    this.send(to, subject, body);
    console.log(`[${this.name}] Сповіщення надіслано`);
  }
}
```

**4.2.** Клас `EmailNotifier extends BaseNotifier`:

- Конструктор: `constructor(private readonly smtpServer: string)` — викликає `super("Email")`
- Реалізація `send()` — виводить повідомлення у форматі:
  `📧 Email → [to]: "[subject]" | Тіло: [перші 50 символів body] через [smtpServer]`

**4.3.** Клас `SmsNotifier extends BaseNotifier`:

- Конструктор: `constructor(private readonly phonePrefix: string = "+380")` — викликає `super("SMS")`
- Реалізація `send()` — виводить:
  `📱 SMS → [phonePrefix][to]: "[перші 160 символів body]"`

**4.4.** Функція `sendBulkNotification(notifiers: BaseNotifier[], to: string, subject: string, body: string): void` — надсилає сповіщення через усі канали.

Продемонструйте поліморфізм:

```typescript
console.log("=== Завдання 4: Наслідування та поліморфізм ===");

const notifiers: BaseNotifier[] = [
  new EmailNotifier("smtp.gmail.com"),
  new SmsNotifier(),
];

// Параметр to — рядок-адресат; його формат залежить від каналу
sendBulkNotification(
  notifiers,
  "user@example.com",
  "Нова задача призначена",
  "Вам призначено задачу 'Розробити API' з пріоритетом high. Дедлайн: 01.02.2025",
);
```

---

#### **Завдання 5: Type Guards та звуження типів (task-5.ts)**

Першим рядком файлу додайте `export {};`. Оголосіть типи `Status`, `Priority` та інтерфейс `Task`.

**5.1.** Оголосіть discriminated union для стану завантаження даних:

```typescript
type LoadingState = { status: "loading" };
type SuccessState<T> = { status: "success"; data: T; loadedAt: Date };
type ErrorState = { status: "error"; message: string; code: number };

type FetchState<T> = LoadingState | SuccessState<T> | ErrorState;
```

**5.2.** Реалізуйте type guard функції:

- `isLoadingState(state: FetchState<unknown>): state is LoadingState`
- `isSuccessState<T>(state: FetchState<T>): state is SuccessState<T>`
- `isErrorState(state: FetchState<unknown>): state is ErrorState`

**5.3.** Реалізуйте функцію `renderState<T>(state: FetchState<T>, renderData: (data: T) => string): string`, що повертає рядок залежно від стану:

- `"loading"` → `"⏳ Завантаження..."`
- `"success"` → `"✅ Завантажено о [час]: " + renderData(state.data)`
- `"error"` → `"❌ Помилка [code]: [message]"`

**5.4.** Реалізуйте функцію `processValue(value: string | number | boolean | null | undefined): string`, що використовує `typeof` та перевірку на `null`/`undefined` для повернення рядку:

- `null` або `undefined` → `"(порожнє значення)"`
- `string` → `"Рядок: '[value]' (${довжина} символів)"`
- `number` → `"Число: [value] (${парне/непарне})"`
- `boolean` → `"Булеве: ${true → 'так' / false → 'ні'}"`

**5.5.** Реалізуйте exhaustive check: функція `getStatusLabel(status: Status): string` обробляє всі варіанти `Status` через `switch/case`. Блок `default` має містити перевірку через `never`, що гарантує обробку всіх випадків.

Продемонструйте роботу:

```typescript
console.log("=== Завдання 5: Type Guards та звуження типів ===");

const states: FetchState<Task[]>[] = [
  { status: "loading" },
  { status: "success", data: [], loadedAt: new Date() },
  { status: "error", message: "Not found", code: 404 },
];

states.forEach((state) => {
  console.log(renderState(state, (tasks) => `${tasks.length} задач`));
});

// Демонстрація processValue
const values: (string | number | boolean | null | undefined)[] = [
  "TypeScript",
  42,
  true,
  null,
  undefined,
  0,
  "",
];
values.forEach((v) => console.log(processValue(v)));
```

---

### **Етап 3: Публікація на GitHub**

3.1. Переконайтесь, що код кожного завдання компілюється без помилок:

```bash
cd lab2
npx tsc
```

3.2. Переконайтесь, що кожне завдання має окремий коміт:

```bash
git log --oneline
```

Приклад очікуваної історії комітів:

```
a1b2c3d Lab 2: Task 5 - Type guards and type narrowing
b4c5d6e Lab 2: Task 4 - Inheritance and polymorphism
c7d8e9f Lab 2: Task 3 - Classes and access modifiers
d0e1f2a Lab 2: Task 2 - Generics and utility types
e3f4a5b Lab 2: Task 1 - Interfaces and type aliases
f6a7b8c Lab 2: Initial TypeScript project structure
```

3.3. Заповніть `lab2/README.md`:

```markdown
# Лабораторна робота №2 — Типізація проекту

**Варіант:** [номер]

## Структура проєкту

- `src/task-1.ts` — Базові типи, інтерфейси та type aliases
- `src/task-2.ts` — Generics та Utility Types
- `src/task-3.ts` — Класи та модифікатори доступу
- `src/task-4.ts` — Наслідування та поліморфізм
- `src/task-5.ts` — Type Guards та звуження типів

## Запуск

1. Встановіть залежності: `npm install`
2. Скомпілюйте TypeScript: `npx tsc`
3. Запустіть завдання: `node dist/task-N.js`
```

3.4. Відправте всі зміни на GitHub:

```bash
git push origin main
```

**Результат:** Репозиторій містить усі завдання з описовою історією комітів, а компіляція завершується без помилок.

## **Зміст звіту**

1. **Титульна сторінка** з темою, метою роботи, обладнанням, програмним забезпеченням

2. **Відповіді на контрольні питання** (коротко, по суті)

3. **Виконання завдань:**

   3.1. Скріншот структури репозиторію на GitHub (папка `lab2` з файлами)

   3.2. Для кожного завдання (1–5): скріншот коду та скріншот результату виконання в терміналі

   3.3. Скріншот виводу `npx tsc` без помилок

   3.4. Скріншот історії комітів (вкладка Commits на GitHub)

4. **Посилання** на ваш GitHub репозиторій

5. **Висновки** (що навчились робити, які навички отримали, з якими труднощами зіткнулись)

## **Типові помилки та їх вирішення**

| Проблема                                      | Рішення                                                                                        |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `Duplicate identifier 'Status'` або `'Task'`  | Файл не є модулем — додайте `export {};` першим рядком файлу                                   |
| `Cannot find module './config'`               | Перевірте, що файл `config.ts` існує і містить `export const VARIANT`                          |
| `Object is possibly 'null'` або `'undefined'` | Увімкнено `strict: true` — додайте явну перевірку: `if (value !== null)` або використайте `??` |
| `Type 'X' is not assignable to type 'Y'`      | Перевірте відповідність типів. Не використовуйте `as any` для обходу — знайдіть правильний тип |
| `Property 'X' does not exist on type 'Y'`     | Властивість не описана в інтерфейсі — додайте її або використайте правильний тип               |
| `abstract` клас не можна інстанціювати        | `new BaseNotifier()` — помилка. Створюйте екземпляри лише конкретних нащадків                  |

## **Додаткові матеріали**

1. TypeScript Handbook — https://www.typescriptlang.org/docs/handbook/

2. Сучасний підручник JavaScript (основи, на яких базується TS) — https://uk.javascript.info/

3. JavaScript Tutorial (W3Schools, українською) — https://w3schoolsua.github.io/js/index.html

4. TypeScript Tutorial (W3Schools, українською) — https://w3schoolsua.github.io/typescript/index.html
